/**
 * Константи адмін-інтерфейсу та пов’язаних списків.
 * Згруповано за доменами; спільні значення винесені в «Загальне».
 */

// =============================================================================
// Загальне (глобальний адмін, пошук у таблицях)
// =============================================================================

/** Дебаунс текстового пошуку в таблицях (мс). */
export const ADMIN_LIST_SEARCH_DEBOUNCE_MS = 350;

// =============================================================================
// Станції
// =============================================================================

/** Розмір сторінки списку станцій (пагінація API та `StationsContext`). */
export const DEFAULT_STATION_PAGE_SIZE = 50;

/**
 * Максимум точок за один запит карти в межах видимого bbox (`fetchStationsMapBounds`).
 * Збігається з лімітом клієнта в `api/stations` (map).
 */
export const STATION_MAP_VIEWPORT_FETCH_LIMIT = 1000;

// =============================================================================
// Бронювання / сесії / платежі — мережеві таблиці глобального адміна
// =============================================================================

/** Однаковий розмір сторінки для великих таблиць (бронювання, сесії, платежі). */
export const GLOBAL_ADMIN_NETWORK_TABLE_PAGE_SIZE = 50;
